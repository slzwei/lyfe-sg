# Migrations are managed in lyfe-app

All database migrations live in `lyfe-app/supabase/migrations/`.
Do NOT add migration files here — a CI check will block PRs that do.

See `INTEGRATION_PLAN.md` in the root `lyfe/` directory for details.
