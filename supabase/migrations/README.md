# Migrations

All Supabase migrations live in `lyfe-app/supabase/migrations/` (the canonical source).

Do NOT add migration files here. The numbered files previously in this directory (001-011) were the original pre-consolidation migrations and have been archived to `../_legacy/`.

To create a new migration, work in the `lyfe-app/` directory.
